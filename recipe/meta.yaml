{% set name = "anesthetic" %}
{% set version = "2.13.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/anesthetic-{{ version }}.tar.gz
  sha256: 4a27b57f2fa552f418aa41efc59f02ba003bf740dd7d01101224399a3e560220

build:
  entry_points:
    - anesthetic = anesthetic.scripts:gui
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - scipy <2.0.0
    - numpy >=1.26.0,<3.0.0
    - pandas >=2.2.0,<2.4.0
    - matplotlib-base >=3.6.1,<3.11.0

test:
  imports:
    - anesthetic
  commands:
    - pip check
    - anesthetic --help
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: nested sampling post-processing
  home: https://github.com/handley-lab/anesthetic
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - beckermr
