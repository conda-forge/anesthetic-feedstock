{% set name = "anesthetic" %}
{% set version = "2.14.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/anesthetic-{{ version }}.tar.gz
  sha256: d16008eedea2b62318735d30562264053c9a08e796813031100215893484cd19

build:
  entry_points:
    - anesthetic = anesthetic.scripts:gui
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - scipy <2.0.0
    - numpy >=1.26.0,<3.0.0
    - pandas >=2.2.0,<2.4.0
    - matplotlib-base >=3.6.1,<3.11.0

test:
  imports:
    - anesthetic
  commands:
    - pip check
    - anesthetic --help
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: nested sampling post-processing
  home: https://github.com/handley-lab/anesthetic
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - beckermr
